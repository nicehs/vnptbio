apiVersion: v1
kind: Service
metadata:
  name: docker-registry
  namespace: default
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: docker-registry
  selector:
    app: docker-registry